# Pathwise — environment variables
# Fill these in via Lovable's secrets manager (or a local .env), not by pasting into code.
# Both are optional in the prototype; when unset, the app returns a mock roadmap.
VITE_AI_API_KEY=
VITE_AI_API_URL=